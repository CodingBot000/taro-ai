# Backend Licenses And Sources

## Application Code

Project Spring Boot application code. Add the final repository license before public submission.

## Dependencies

Java dependencies are resolved from Maven Central through Gradle. The dependency graph is reproducible through `build.gradle` and Gradle's dependency management.
